# Optional: copy to .env.local for web-only overrides.
# Repo-root ../.env is loaded first (shared with Python), then web/.env* via Next.
MONGODB_URI=mongodb+srv://USER:PASS@cluster.mongodb.net/?retryWrites=true&w=majority
MONGODB_DB=wattsup

# Default dashboard user (Mongo user_id on energy_logs / user_stats)
WATTSUP_DEFAULT_USER_ID=test-user

# Exposed to browser — optional default for the user id field
NEXT_PUBLIC_WATTSUP_USER_ID=test-user

# Fast chat (Ask WattsUp): OpenAI-compatible gateway + flash model (see repo ../.env K2V2_* / GEMINI_FLASH_MODEL).
# Bill analysis still uses K2_ENDPOINT + K2_MODEL (Think v2).
# K2V2_BASE_URL=https://api.lava.so
# K2V2_API_KEY=
# GEMINI_FLASH_MODEL=gpt-4o-mini
